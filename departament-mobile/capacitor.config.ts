import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '/workspaces/miami-metro/departament-mobile',
  webDir: 'www'
};

export default config;
